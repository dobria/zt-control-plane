# Blocked public preview

ZT Control Plane `v0.1.0` is a protected source tag, not a published release.
The release workflow stopped at its critical-vulnerability gate before it
created a container package or GitHub Release. The remediation is published as
`v0.1.1`.

Do not deploy `v0.1.0`. Use the first published release, `v0.1.1`, instead.
